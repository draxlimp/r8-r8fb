export interface RoleSnapshot { id:string; name:string; color:number; position:number; permissions:string; hoist:boolean; mentionable:boolean; icon:string|null; unicodeEmoji:string|null; capturedAt:string; version:number }
export function captureRole(role:any):RoleSnapshot{return {id:role.id,name:role.name,color:role.color,position:role.position,permissions:role.permissions.bitfield.toString(),hoist:role.hoist,mentionable:role.mentionable,icon:role.icon??null,unicodeEmoji:role.unicodeEmoji??null,capturedAt:new Date().toISOString(),version:1};}
